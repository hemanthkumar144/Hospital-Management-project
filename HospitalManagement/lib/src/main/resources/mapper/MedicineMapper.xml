<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crimsonlogic.hospitalmanagement.mapper.IMedicineMapper">

    <resultMap id="medicineResultMap"
               type="com.crimsonlogic.hospitalmanagement.model.Medicine">

        <id property="medicineId"
            column="medicine_id"/>

        <result property="medicineName"
                column="medicine_name"/>

        <result property="manufacturer"
                column="manufacturer"/>

        <result property="price"
                column="price"/>

        <result property="active"
                column="active"/>

    </resultMap>


    <insert id="addMedicine">

        INSERT INTO medicine
        (
            medicine_id,
            medicine_name,
            manufacturer,
            price,
            active
        )
        VALUES
        (
            #{medicineId},
            #{medicineName},
            #{manufacturer},
            #{price},
            #{active}
        )

    </insert>


    <select id="getMaxMedicineNumber"
            resultType="java.lang.Integer">

        SELECT MAX(
            CAST(
                SUBSTRING(medicine_id, 4)
                AS UNSIGNED
            )
        )

        FROM medicine

    </select>


    <select id="getMedicineById"
            resultMap="medicineResultMap">

        SELECT
            medicine_id,
            medicine_name,
            manufacturer,
            price,
            active

        FROM medicine

        WHERE medicine_id = #{medicineId}
          AND active = TRUE

    </select>


    <select id="getAllMedicines"
            resultMap="medicineResultMap">

        SELECT
            medicine_id,
            medicine_name,
            manufacturer,
            price,
            active

        FROM medicine

        WHERE active = TRUE

        ORDER BY medicine_name

    </select>


    <update id="updateMedicine">

        UPDATE medicine

        SET medicine_name = #{medicineName},
            manufacturer = #{manufacturer},
            price = #{price}

        WHERE medicine_id = #{medicineId}
          AND active = TRUE

    </update>


    <update id="deleteMedicine">

        UPDATE medicine

        SET active = FALSE

        WHERE medicine_id = #{medicineId}
          AND active = TRUE

    </update>

</mapper>